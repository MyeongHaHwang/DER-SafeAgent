# Rule-based IDS Baseline

- `rules/` — Suricata/Zeek rules covering FDI, replay, command spoof signatures.
- `adapter.py` — wraps rule engine to the `detect()` interface.
- Reference: open-source SCADA/DER rule sets (cite in paper).
